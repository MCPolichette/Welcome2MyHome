<div id="home">
  <div class="container">
    <div class="row">
        <h3>Houses List:</h3>
        <br>
        <br>
        <ul id="house-list" class="list-group">
            {{#each houses}}
          <li data-id="{{this.id}}" class="list-group-item">
            <a href="house/{{this.id}}">  Host Name:  {{this.host_name}}</a>
            <button class="btn btn-danger float-right delete">ｘ</button>
          </li>
          {{/each}}
        </ul>
      </div>
    </div>
  </div>
</div>

